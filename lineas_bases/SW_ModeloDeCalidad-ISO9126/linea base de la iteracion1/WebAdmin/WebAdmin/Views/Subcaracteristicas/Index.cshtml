@model IEnumerable<WebAdmin.Models.Subcaracteristica>

@{
    ViewData["Title"] = "Index";
}

<h2>SubCarácteristicas</h2>

<p>
    <a class="btn btn-primary" asp-action="Create">Nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdCaracteristicaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdCaracteristicaNavigation.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSubcaracteristica">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdSubcaracteristica">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSubcaracteristica">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
